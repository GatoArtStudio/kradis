services:
  kradis:
    image: kradis
    build:
      context: .
      dockerfile: Kradis/Dockerfile
    env_file:
      - ./.env
    volumes:
      - kradis-logs:/app/data/logs

volumes:
  kradis-logs: